#include <queue>

struct Die {
     int sides;
     int result;
};

void add_dice(int numDice, int numSides);
void set_sum_modifier(int modifier);
void set_die_modifier(int modifier)
std::queue<die> get_dice();
int get_sum();


